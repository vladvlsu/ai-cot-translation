**HERE IS LITLE GUIDE OF HOW TO USE THIS CODE**

  - create the env:
    ```
    conda create -n "envname" python=3.10 -y
    conda acrivate envname
    ```

  - install the dependencies from 'requirements.txt' file:
    ```
    pip install -r requirements.txt
    ```

  - put the files for translation into 'to_trnslt' folder
  - run the code by:
    ```
    py app.py
    ```

  - choose the way of translation in the traminal dialogue – weather its ENG to RUS or RUS to ENG

  - wait till translation is done
